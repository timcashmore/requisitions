_schema-version: "3.2"
ID: tcrequisitions
description: Generated by Fiori Tools
version: 1.0.11
modules:
- name: tcrequisitions-destination-content
  type: com.sap.application.content
  requires:
  - name: tcrequisitions-destination-service
    parameters:
      content-target: true
  - name: tcrequisitions-repo-host
    parameters:
      service-key:
        name: tcrequisitions-repo-host-key
  - name: tcrequisitions-uaa
    parameters:
      service-key:
        name: tcrequisitions-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: tcrequisitions_html_repo_host
          ServiceInstanceName: tcrequisitions-html5-srv
          ServiceKeyName: tcrequisitions-repo-host-key
          sap.cloud.service: tcrequisitions
        - Authentication: OAuth2UserTokenExchange
          Name: tcrequisitions_uaa
          ServiceInstanceName: tcrequisitions-xsuaa-srv
          ServiceKeyName: tcrequisitions-uaa-key
          sap.cloud.service: tcrequisitions
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: tcrequisitions-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: tcrequisitions-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - tcrequisitions.zip
      name: tcrequisitions
      target-path: resources/
- name: tcrequisitions
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: tcrequisitions-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.11
    service: destination
    service-name: tcrequisitions-destination-service
    service-plan: lite
- name: tcrequisitions-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: tcrequisitions-xsuaa-srv
    service-plan: application
- name: tcrequisitions-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: tcrequisitions-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
